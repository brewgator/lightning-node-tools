# Copy-Paste Ready Telegram Monitor Service
# Note: This is typically run via cron every 2 minutes, not as a persistent service
# Usage:
# 1. Copy this file: sudo cp deployment/systemd/lightning-telegram-monitor.service.example /etc/systemd/system/lightning-telegram-monitor.service
# 2. Edit paths to match your setup: sudo nano /etc/systemd/system/lightning-telegram-monitor.service  
# 3. Reload systemd: sudo systemctl daemon-reload
# 4. For manual testing: sudo systemctl start lightning-telegram-monitor

[Unit]
Description=Lightning Node Telegram Monitor
After=network.target
Wants=network-online.target

[Service]
Type=oneshot
User=YOUR_USERNAME
Group=YOUR_USERNAME
WorkingDirectory=$HOME/lightning-node-tools
ExecStart=$HOME/lightning-node-tools/bin/telegram-monitor
StandardOutput=journal
StandardError=journal

# Security settings
NoNewPrivileges=yes
PrivateTmp=yes
ProtectSystem=strict
ReadWritePaths=$HOME/lightning-node-tools/data
ProtectHome=read-only

[Install]
WantedBy=default.target